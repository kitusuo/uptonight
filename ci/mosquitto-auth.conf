# No authentication backend + anonymous disabled => every connection is
# rejected with "not authorized". That is the CONNACK rejection the smoke test
# needs, without a password file.
listener 1883
allow_anonymous false

max_packet_size 67108864
message_size_limit 67108864
