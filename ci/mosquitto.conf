listener 1883
allow_anonymous true

# The sky-plot image is published as a single multi-MB MQTT message; raise the
# limits so the broker does not drop the connection on it.
max_packet_size 67108864
message_size_limit 67108864
